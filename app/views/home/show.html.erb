<!DOCTYPE html>
<h1> Toutes les patrouilles </h1>
<div class="row">
  <% Troop.all.each do |t|%>
  <div class="col-sm-4">
    <div class="card" style="width: 22rem; margin-bottom: 20px; background-color:lightgrey;">
      <div class="card-body">
        <%= image_tag("#{t.city.name}.png", alt: "#{t.city.name}.png", style: 'height:100px; width:100px;') %>

        <h5 class="card-title"><%= t.name %> <span style="float:right"> score: <%= t.turns_holding_paris %> </span> </h5>
        <% if t.hold_paris? %>
          <b> Tient Paris </b>
        <% end %>
        <hr>
        <% t.patrols.sort_by(&:total_gains).reverse.each do |p|%>
        <p>
          <%= link_to patrol_path(p.id) do %>
            <%= p.name %> <span style="float:right;">score: <%= p.total_gains %></span>
          <% end %>
        </p>
        <% end %>
      </div>
    </div>
  </div>
  <% end %>
</div>

<%= link_to 'finir le tour', end_turn_path, class: 'btn btn-primary' %>
