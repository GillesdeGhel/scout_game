<!DOCTYPE html>

<div class="row">
  <% City.all.each do |c|%>
  <div class="col-sm-4">
    <div class="card" style="width: 22rem; margin-bottom: 20px; background-color:lightgrey;">
      <div class="card-body">
        <%= image_tag("#{c.name}.png", alt: "#{c.name}.png", style: 'height:100px; width:100px;') %>
        <% if c.paris? %>
          <h5 class="card-title"><%= c.name %> - <%= Troop.hold_paris&.name %> </h5>
        <% else %>
          <h5 class="card-title"><%= c.name %> - <%= c.troop&.name %> </h5>
        <% end %>
        <% if c.pillaged %>
        <p style="background-color:red; color: lightgrey; padding: 6px;">
          La ville est tombée au tour passée
        </p>
        <% else %>
        <p style="background-color:green; color: lightgrey; padding: 6px;">
          La ville s'est défendue au tour passée
        </p>
        <% end %>
        <p class="card-text">Puissance des fortifications: x <%= c.defense_building_multiplicator + 1 %> </p>
        <p class="card-text">La ville a été pillée <%= c.pillage_count%> fois </p>
      </div>
    </div>
  </div>
  <% end %>
</div>
